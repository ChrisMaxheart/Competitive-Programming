#include <bits/stdc++.h>

using namespace std;


int gold(int know[], int gold[], int start, int end, int no)
{
	if (start == end)
		if 
	return cost
}

int main ()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    return 0;
}